# Required secrets
ORCHEO_POSTGRES_PASSWORD=change-me
ORCHEO_VAULT_ENCRYPTION_KEY=replace-with-64-hex-chars
VITE_ORCHEO_CHATKIT_DOMAIN_KEY=domain_pk_replace_me
ORCHEO_AUTH_BOOTSTRAP_SERVICE_TOKEN=change-me

# Optional defaults
ORCHEO_POSTGRES_USER=orcheo
ORCHEO_POSTGRES_DB=orcheo
ORCHEO_AUTH_MODE=required
ORCHEO_CHATKIT_PUBLIC_BASE_URL=http://localhost:5173
VITE_ORCHEO_BACKEND_URL=http://localhost:8000
ORCHEO_CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# OAuth Authentication Configuration (Backend - Token Validation)
# These settings are used by the backend to validate incoming OAuth tokens.
# ORCHEO_AUTH_ISSUER: Your OAuth provider's issuer URL (e.g., https://your-domain.auth0.com/)
#                     Must include trailing slash as per OAuth/OIDC spec
# ORCHEO_AUTH_AUDIENCE: API identifier/audience for token validation (e.g., https://api.yourapp.com)
# ORCHEO_AUTH_JWKS_URL: JSON Web Key Set URL for token verification
#                       Usually at {issuer}/.well-known/jwks.json
ORCHEO_AUTH_ISSUER=https://your-auth-provider.com/
ORCHEO_AUTH_AUDIENCE=https://your-api-audience.com
ORCHEO_AUTH_JWKS_URL=https://your-auth-provider.com/.well-known/jwks.json

# OAuth Authentication Configuration (CLI/SDK - Login Flow)
# Used by the Orcheo CLI to initiate OAuth login. Not needed by backend services.
# ORCHEO_AUTH_CLIENT_ID: OAuth application client ID from your provider
# ORCHEO_API_URL: Base URL for the Orcheo API server (e.g., http://localhost:8000)
#                 Used by CLI/SDK to communicate with the backend
# ORCHEO_AUTH_SERVICE_TOKEN: Service token for CLI authentication
ORCHEO_AUTH_CLIENT_ID=your-client-id
ORCHEO_API_URL=http://localhost:8000
ORCHEO_AUTH_SERVICE_TOKEN=change-me

# OAuth Authentication Configuration (Frontend)
# Must match backend values where applicable
# VITE_ORCHEO_AUTH_ISSUER: OAuth provider's issuer URL (must match backend, including trailing slash)
# VITE_ORCHEO_AUTH_CLIENT_ID: OAuth client ID (must match backend)
# VITE_ORCHEO_AUTH_REDIRECT_URI: URL where OAuth provider redirects after authentication
#                                (e.g., http://localhost:5173/callback)
# VITE_ORCHEO_AUTH_SCOPES: OAuth scopes to request (space-separated, defaults to "openid profile email")
# VITE_ORCHEO_AUTH_AUDIENCE: API audience for frontend tokens (should match backend)
# VITE_ORCHEO_AUTH_ORGANIZATION: Organization ID for multi-tenant OAuth providers (e.g., org_xxxxxxxxxx for Auth0)
# VITE_ORCHEO_AUTH_PROVIDER_PARAM: Optional query parameter name for provider selection
# VITE_ORCHEO_AUTH_PROVIDER_GOOGLE: Value for Google OAuth provider selection
# VITE_ORCHEO_AUTH_PROVIDER_GITHUB: Value for GitHub OAuth provider selection
VITE_ORCHEO_AUTH_ISSUER=https://your-auth-provider.com/
VITE_ORCHEO_AUTH_CLIENT_ID=your-client-id
VITE_ORCHEO_AUTH_REDIRECT_URI=http://localhost:5173/callback
VITE_ORCHEO_AUTH_SCOPES=openid profile email
VITE_ORCHEO_AUTH_AUDIENCE=https://your-api-audience.com
VITE_ORCHEO_AUTH_ORGANIZATION=org_xxxxxxxxxx
VITE_ORCHEO_AUTH_PROVIDER_PARAM=connection
VITE_ORCHEO_AUTH_PROVIDER_GOOGLE=google-oauth2
VITE_ORCHEO_AUTH_PROVIDER_GITHUB=github
